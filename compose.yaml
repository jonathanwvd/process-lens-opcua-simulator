services:
  simulator:
    build: .
    read_only: true
    cap_drop: ["ALL"]
    security_opt: ["no-new-privileges:true"]
    ports:
      - "127.0.0.1:4840:4840"
    volumes:
      - simulator-data:/data
    tmpfs:
      - /tmp
    restart: unless-stopped

volumes:
  simulator-data:
